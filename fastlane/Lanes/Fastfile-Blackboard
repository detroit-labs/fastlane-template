desc "Run Blackboard using Mint from the root of the project."
lane :blackboard do
  Dir.chdir("..") do
    if File.exist?(".blackboard.yml")
        sh "mint run blackboard"
      else
        UI.error("Blackboard configuration file not found, please create one. (.blackboard.yml)")
      end
  end
end
